import React from "react";

function NavBar() {
  return (
  // update the JSX being returned!
  <nav>
    <a href="#home">Home</a>
    <a href="#about">About</a>
  </nav>
  )
}

export default NavBar;
